avoid using excessive not use underscore-prefixed (_abc _ABC) naming style, only use for private class method if the styling of the reset allows for it
don't import stuff in middle of the code
avoid using global variables like: `global x`, if you need it, probably you code is bad
use small git commits for things you change, i don't like big ass commits
