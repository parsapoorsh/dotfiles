avoid using excessive not use underscore-prefixed (_abc _ABC) naming style, only use for private class method if the styling of rest of the code allows for it
don't import stuff in middle of the code
avoid using global variables like: `global x`, if you need it, probably your code is bad
use small git commits for things you change, i don't like big ass commits
for functions with multiple args / kwargs, instead of putting all args in a single line, put each arg on a new line to improve readability 
